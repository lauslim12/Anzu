/**
 * Responses for 'Behavior' entity.
 */
const responses = {
  /**
   * Response if Anzu leaves.
   *
   * @returns A message
   */
  leave: () => `Thank you! I will be taking my leave now. Bye-bye!`,
};

export default responses;
